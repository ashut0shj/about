// ParticlesBackground.jsx
// This component is now a no-op re-export stub.
// The lightweight StarCanvas is built directly inside Hero.jsx to avoid
// the heavy tsparticles-slim bundle (~200KB gzipped).
//
// If you still need tsparticles elsewhere, restore the original.
// Otherwise, you can safely delete this file and remove its imports.

const ParticlesBackground = () => null;
export default ParticlesBackground;
